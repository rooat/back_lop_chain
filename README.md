<!-- const schema1 = new mongoose.Schema({
    address : String,
    data : String,
    timestamps:String,
    endtime:String,
    txhash:String,
    state:Number,
    username:String,
    password:String
});
var userData = mongoose.model("userData",schema1);
var withdrawData = mongoose.model("withdrawData",schema1); -->